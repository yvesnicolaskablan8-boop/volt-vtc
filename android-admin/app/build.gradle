plugins {
    id 'com.android.application'
}

android {
    namespace 'com.volt.admin'
    compileSdk 34

    defaultConfig {
        applicationId "com.volt.admin"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0.0"
    }

    signingConfigs {
        release {
            storeFile file("../volt-admin.keystore")
            storePassword "voltadmin2024"
            keyAlias "volt-admin"
            keyPassword "voltadmin2024"
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            debuggable true
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
}
